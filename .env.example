# WorkBrew — Environment Variables
# Copy this file to .env and fill in your values. Never commit .env.

SECRET_KEY=your-random-secret-key-here

# Admin credentials (session-based auth — no user table)
ADMIN_USER=admin
ADMIN_PASS=your-admin-password-here

# Leave blank for local SQLite development.
# On Render.com: paste the Internal Database URL from your PostgreSQL service.
DATABASE_URL=

# PostgreSQL schema name for table isolation (e.g. "workbrew").
# Leave blank for local SQLite development — schemas are Postgres-only.
# On Render.com: set to "workbrew" (already set in render.yaml).
DB_SCHEMA=
